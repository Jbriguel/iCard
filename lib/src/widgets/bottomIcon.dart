import 'package:flutter/material.dart';

Widget buildBottomIcon(Color mainTextColor) {
  IconData icon = Icons.keyboard_arrow_down;
  return Icon(
    icon,
    color: mainTextColor,
    size: 15,
  );
}
